spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.cleanarch.cn:80
        namespace: dp-test
      config:
        server-addr: nacos.cleanarch.cn:80
        namespace: dp-test
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}
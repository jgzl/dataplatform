spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: dp-dev
      config:
        server-addr: nacos.oracle.cleanarch.cn:80
        namespace: dp-dev
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}
spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.cleanarch.cn:8848
        namespace: gw-prod
      config:
        server-addr: nacos.cleanarch.cn:8848
        namespace: gw-prod
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}
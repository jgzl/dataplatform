spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.cleanarch.cn:8848
        namespace: dp-dev
      config:
        server-addr: nacos.cleanarch.cn:8848
        namespace: dp-dev
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}
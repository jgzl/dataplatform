spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.cleanarch.cn:8848
        namespace: dp-prod
      config:
        server-addr: nacos.cleanarch.cn:8848
        namespace: dp-prod
        file-extension: yml
        shared-configs:
          - application.${spring.cloud.nacos.config.file-extension}